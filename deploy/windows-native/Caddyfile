{
    # Cloudflare ???? HTTPS?Caddy ????? 80 ?????
    auto_https off
    admin off
}

# ????????????? FastAPI?
http://portal.aim888888.xyz {
    reverse_proxy 127.0.0.1:8000 {
        header_up Host {host}
        # ???? Cloudflare ? HTTPS???????? HTTP?
        # ???????????????? HTTPS?????????? Cookie ???
        header_up X-Forwarded-Proto https
    }
}

# ?????????? Forward Auth????? frps ? HTTP vhost ???
http://*.aim888888.xyz {
    forward_auth 127.0.0.1:8000 {
        uri /api/auth
        copy_headers X-Auth-User X-Auth-User-Id X-Auth-Project X-Auth-Access-Type

        # ?????????? FastAPI?
        header_up Host portal.aim888888.xyz
        header_up X-Forwarded-Host {host}
        header_up X-Forwarded-Method {method}
        header_up X-Forwarded-Uri {uri}
        header_up X-Forwarded-Proto https
        header_up X-Portal-Auth-Redirect 1
    }

    # ????????? Host ??????? frps?
    # frps ??? customDomains ? HTTP ???????
    reverse_proxy 127.0.0.1:8080 {
        header_up Host {host}
        header_up X-Forwarded-Proto https
    }
}
